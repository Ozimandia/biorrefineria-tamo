[build]
  command = "npm run build"
  publish = "dist"

# Redirección para Single Page Application (SPA) - Asegura que todas las rutas carguen el index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200