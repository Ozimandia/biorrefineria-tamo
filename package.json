{
  "name": "biorrefineria-tamo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "check": "node --check calculos.js && node --check graficas.js && node --check proceso.js",
    "test": "node --test tests/*.test.cjs"
  }
}
